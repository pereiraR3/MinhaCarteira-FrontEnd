import {AbstractFiltro} from './abstract-filtro.model';

export class UnidadeFiltro extends AbstractFiltro{

  nome: string; //nome da unidade ainda não está implementado no back-end
  parceiroId:number
  somenteAtivos: boolean;

}

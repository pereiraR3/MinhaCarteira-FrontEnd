import { NumericInputDirective } from './numeric-input';

describe('NumericInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericInputDirective(null as any);
    expect(directive).toBeTruthy();
  });
});
